---
import { getLocale, useTranslations, localizePath } from '../i18n/utils';

const path = Astro.url.pathname;
const locale = getLocale(Astro.url);
const t = useTranslations(locale);

const nav = [
  { href: '/projets', key: 'nav.projets' as const },
  { href: '/journal', key: 'nav.journal' as const },
  { href: '/agence', key: 'nav.agence' as const },
  { href: '/contact', key: 'nav.contact' as const },
];
const isActive = (href: string) => {
  const localized = localizePath(href, locale);
  return path === localized || path.startsWith(localized + '/');
};
---
<header class="site-header">
  <div class="container bar">
    <a href={localizePath('/', locale)} class="brand plain" aria-label="Agence L&L">
      Agence&nbsp;L<span class="amp">&amp;</span>L
    </a>

    <nav aria-label="Navigation principale">
      <ul>
        {nav.map((item) => (
          <li>
            <a
              href={localizePath(item.href, locale)}
              class="nav-link plain"
              aria-current={isActive(item.href) ? 'page' : undefined}
            >{t(item.key)}</a>
          </li>
        ))}
      </ul>
    </nav>

    <a href={localizePath('/contact', locale)} class="rdv plain">Prendre rendez-vous</a>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 50;
    background: rgba(246, 242, 235, 0.9); /* = --paper à 90% */
    backdrop-filter: saturate(1.2) blur(8px);
    -webkit-backdrop-filter: saturate(1.2) blur(8px);
    border-bottom: var(--rule);
  }
  .bar {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: var(--space-m);
    min-height: 4.75rem;
  }

  /* Mot-marque — Cormorant Garamond */
  .brand {
    justify-self: start;
    font-family: var(--font-display);
    font-size: 1.65rem;
    font-weight: 600;
    letter-spacing: 0.01em;
    color: var(--ink);
    white-space: nowrap;
  }
  .brand .amp { font-style: italic; }

  /* Navigation centrale — IBM Plex Mono */
  nav { justify-self: center; }
  nav ul {
    display: flex;
    gap: clamp(1.1rem, 3vw, 2.4rem);
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .nav-link {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--ink-soft);
    padding-block: 0.35rem;
    border-bottom: 1.5px solid transparent;
    white-space: nowrap;
    transition: border-color 0.2s var(--ease), color 0.2s var(--ease);
  }
  .nav-link:hover { color: var(--ink); }
  .nav-link[aria-current='page'] { border-color: var(--terracotta); color: var(--ink); }

  /* Bouton « Prendre rendez-vous » — contour, mono */
  .rdv {
    justify-self: end;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink);
    border: 1px solid var(--ink);
    padding: 0.6rem 1rem;
    white-space: nowrap;
    transition: background-color 0.2s var(--ease), color 0.2s var(--ease);
  }
  .rdv:hover { background: var(--ink); color: var(--paper); }

  /* Responsive : on replie la nav et le bouton sous le mot-marque */
  @media (max-width: 60rem) {
    .bar { grid-template-columns: 1fr auto; }
    nav { display: none; }
  }
  @media (max-width: 30rem) {
    .rdv { font-size: 0.62rem; padding: 0.5rem 0.7rem; }
  }
</style>
